# The secret key for admin operations.
# Replace with a long, random, and secure key.
ADMIN_SECRET_KEY=your_super_secret_key

# Firebase Admin SDK Service Account JSON, as a single line string.
# Go to Firebase Console > Project Settings > Service Accounts > Generate new private key
# Then, convert the downloaded JSON file to a single line string.
# e.g., using a tool like https://www.textfixer.com/tools/remove-line-breaks.php
FIREBASE_SERVICE_ACCOUNT_JSON=
